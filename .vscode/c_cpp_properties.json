{
    "env": {
        "program": "helloWorld",
        "build_dir": "bin"
    },
    "configurations": [
        {
            "name": "win",
            "customConfigurationVariables": {
                "program": "${env:build_dir}/win/${env:program}.exe"
            }
        },
        {
            "name": "linux",
            "customConfigurationVariables": {
                "program": "${env:build_dir}/linux/${env:program}"
            }
        },
        {
            "name": "macos",
            "customConfigurationVariables": {
                "program": "${env:build_dir}/macos/${env:program}"
            }
        }
    ],
    "version": 4
}